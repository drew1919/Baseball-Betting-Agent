{
  "name": "baseball-gambling-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "npm run build && node dist/index.js",
    "analyze:history": "node scripts/analyze-history.mjs",
    "calibrate:rotowire": "node scripts/calibrate-rotowire-hold.mjs",
    "validate:model": "npm run build && node scripts/validate-model.mjs"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
